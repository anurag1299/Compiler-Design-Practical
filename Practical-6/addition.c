#include <stdio.h>
#include <math.h>

int a, b;
int main(int argc, char const *argv[])
{
    a = 10;

    scanf("%d", &b);
    int c = a + b;
    printf("addition = %d", c);
    printf("Log = %lf", log(10));

    return 0;
}
